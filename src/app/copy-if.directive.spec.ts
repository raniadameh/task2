import { CopyIfDirective } from './copy-if.directive';

describe('CopyIfDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyIfDirective();
    expect(directive).toBeTruthy();
  });
});
